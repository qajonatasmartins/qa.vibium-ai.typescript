const menuBase = ".shop-menu .nav:first-child li"

export const menuComponents = {
    btnHomeMenu: `${menuBase}:nth-child(1) a`,
    btnProductsMenu: `${menuBase}:nth-child(2) a`,
    btnCartMenu: `${menuBase}:nth-child(3) a`,
    btnSignupLoginMenu: `${menuBase}:nth-child(4) a`
}